// Objects in java Script.
// objects in java script declared as below,---> name:value pair format
const car = {
    name:"Nexon",color:"black",price:36000000,
    getPrice: function() {
        return this.price;
    }
}
// console.log(car.getPrice());
console.log(car.getPrice());

// events in java script